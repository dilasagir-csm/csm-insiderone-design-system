GoalBox is a small checklist card naming the goals an Architect journey is optimizing for.

```jsx
<GoalBox goals={["Goal 1", "Goal 2", "Goal 3"]} />
```

Pairs with `SegmentBox` inside a journey diagram — segment on one side, goals on the other.
