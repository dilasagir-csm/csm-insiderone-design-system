SegmentBox is the journey-builder audience card — a segment name list plus an estimated-reach figure, used inside Architect journey diagrams.

```jsx
<SegmentBox
  segments={["Cart abandoners", "High LTV shoppers"]}
  audience="16K"
/>
```

`audience` is optional — omit it to render a segment list with no reach figure.
